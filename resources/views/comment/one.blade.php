<div class="fs-6 text-white-50" data-id="{{ $comment->id }}">
    <span>{{ $comment->created }}</span>
    <span class="text-warning">{{ $comment->author }}</span>:
    <span>{{ $comment->text }}</span>
</div>